\ProvidesPackage{macros/preambulo}

%%% PAQUETES
\usepackage{amsmath,amsfonts,amssymb,amsthm} %AMS
\usepackage[utf8]{inputenc}
%\usepackage[spanish]{babel}
\usepackage{graphicx}\graphicspath{{../figuras}} % Figuras
\usepackage{macros/math-letters} % paquete personal para abreviar letras
\usepackage{macros/enumitem} % paquete para usar i,ii,iii,... en listas
\usepackage{macros/verbatim}
\usepackage{macros/cite}
\usepackage{tikz-cd}\usetikzlibrary{positioning,graphs,arrows}\usetikzlibrary{decorations.markings}%\usetikzlibrary{decorations.pathmorphing}
\usepackage[symbol]{macros/footmisc}\renewcommand{\thefootnote}{\fnsymbol{footnote}}\counterwithout{footnote}{section} % cambio de simbolos de notas de pie

%%% MARGENES
\addtolength{\hoffset}{-2cm}
\addtolength{\textwidth}{4cm}
\addtolength{\voffset}{-2cm}
\addtolength{\textheight}{4cm}

%%% THEOREM STYLES
\theoremstyle{plain}\newtheorem{thm}{Teorema}
\theoremstyle{plain}\newtheorem{lema}[thm]{Lema}
\theoremstyle{plain}\newtheorem*{conjectura*}{Conjetura}
\theoremstyle{plain}\newtheorem{cor}[thm]{Corolario}
\theoremstyle{definition}\newtheorem{defin}[thm]{Definici\'on}
\theoremstyle{plain}\newtheorem{prop}[thm]{Proposici\'on}
\theoremstyle{remark}\newtheorem*{nota}{Nota}
\theoremstyle{remark}\newtheorem*{notas}{Notas}
\theoremstyle{definition}\newtheorem{ejercicio}[thm]{Ejercicio}
\theoremstyle{definition}\newtheorem{ejemplo}[thm]{Ejemplo}
\theoremstyle{definition}\newtheorem{alg}[thm]{Algoritmo}

%%% FLECHAS
\newcommand{\then}{\Longrightarrow} % =>
\newcommand{\onlyif}{\Longleftarrow} % <=
\newcommand{\ra}{\rightarrow} % ->
\newcommand{\lra}{\longrightarrow} % --->
\newcommand{\rai}{\rightarrow\infty} % ->\infty
\newcommand{\morf}[1]{\overset{#1}{\longrightarrow}} % flecha horizontal etiquetada
\newcommand{\epi}{\twoheadrightarrow} % epimorfismo
\newcommand{\inc}{\hookrightarrow}  % c->
\newcommand{\act}{\curvearrowright}  % flecha de accion

%%% ABREVIACIONES
\newcommand{\paren}[1]{\left( #1 \right)} % Parenthesis
\newcommand{\abs}[1]{\left| #1 \right|} % Valor absoluto
\newcommand{\gen}[1]{\langle #1 \rangle} % Angled braces 
\newcommand{\what}[1]{\widehat{ #1 }} % wide hat
\newcommand{\im}[1]{\mathrm{Im}\left( #1 \right)}  % imagen de una función
\newcommand{\Mod}[1]{\;\;(\mathrm{mod}\; #1)} % (mod #)
\newcommand{\leg}[2]{\left(\frac{#1}{#2}\right)}  % simbolo de legendre
\newcommand{\ol}[1]{\overline{#1}} %% abreviacion de \overline
\newcommand{\mat}[4]{\begin{pmatrix}#1 & #2\\ #3 & #4\end{pmatrix}}  % matriz de 2x2
\newcommand{\matt}[4]{\scriptsize \begin{pmatrix}#1 & #2\\ #3 & #4\end{pmatrix}\normalsize} % matriz chica de 2x2

\newcommand{\Id}{\mathrm{Id}} % Identidad
\newcommand{\GL}{\mathrm{GL}} % General Linear Group
\newcommand{\PGL}{\mathrm{PGL}} % Projective General Linear Group
\newcommand{\PSL}{\mathrm{PSL}} % Projective Special Linear Group
\newcommand{\SL}{\mathrm{SL}} % Special Linear Group
\newcommand{\SO}{\mathrm{SO}} % Special orthogonal Group
\newcommand{\Fr}{\mathrm{Frob}} % Frobenius element
\newcommand{\tr}{\mathrm{tr}} % Traza de una matriz
\renewcommand{\l}{\ell}  % l cursiva para primo
\newcommand{\p}{\mathfrak{p}} % p gotica que representa ideal primo
\newcommand{\q}{\mathfrak{q}} % q gotica que representa ideal primo
\newcommand{\m}{\mathfrak{m}} % m gotica que representa ideal maximal
\newcommand{\st}{\overset{\sim}{\sigma}} % "sigma tilde"
\newcommand{\End}{\mathrm{End}}  % Endomorfismos
\newcommand{\Aut}{\mathrm{Aut}}  % Endomorfismos
\newcommand{\Gal}{\mathrm{Gal}} % grupo de galois
\newcommand{\GQ}{G_{\QQ}} % grupo absoluto de Galois de Q
\newcommand{\Hom}{\mathrm{Hom}} % Hom
\newcommand{\ord}{\mathrm{ord}} % orden

%%% NOTACION
\newcommand{\gag}{\Gamma\alpha\Gamma'} % clase bilateral de dos subgrupos de congruencia Gamma y Gamma'
\newcommand{\rhot}{\bar{\rho}_{E,3}} % representacion asociada a los puntos de 3-torsion de E
\newcommand{\rhoc}{\bar{\rho}_{E,5}} % ... 5-torsion ...
\newcommand{\rhop}{\bar{\rho}_{E,p}} % ... p-torsion ...
\newcommand{\rhoN}{\bar{\rho}_{E,N}} % ... N-torsion ...
\newcommand{\Sk}{S_k(\Gamma_0(N))} % formas cuspidales de Gamma_0(N) de peso k
\newcommand{\Snew}{S_k^{\mathrm{new}}(\Gamma_0(N))} % formas primitivas de Gamma_0(N) de peso k
\newcommand{\Snewx}{S_k^{\mathrm{new}}(\Gamma_0(N),\chi)} % formas primitivas de Gamma_0(N) de peso k con nebetypus chi
\newcommand{\Sold}{S_k^{\mathrm{old}}(\Gamma_0(N))} % oldforms de Gamma_0(N) de peso k
\newcommand{\Soldx}{S_k^{\mathrm{new}}(\Gamma_0(N),\chi)} % oldforms de Gamma_0(N) de peso k con nebentypus chi
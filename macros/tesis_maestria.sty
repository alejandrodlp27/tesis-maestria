\ProvidesPackage{algebra}
% ALGEBRA

% Letters
	\newcommand{\Id}{\mathrm{Id}} %%% Identity function
	\newcommand{\im}[1]{\mathrm{Im}\left( #1 \right)}
	\newcommand{\GL}{\mathrm{GL}} %%% General Linear Group
	\newcommand{\PGL}{\mathrm{PGL}} %%% Projective General Linear Group
	\newcommand{\PSL}{\mathrm{PSL}} %%% Projective Special Linear Group
	\newcommand{\SL}{\mathrm{SL}} %%% Special Linear Group
	\newcommand{\SO}{\mathrm{SO}} %%% Special orthogonal Group
	\newcommand{\Fr}{\mathrm{Frob}} %%% Frobenius element
	\newcommand{\tr}{\mathrm{tr}} %%% Traza de una matriz
        \renewcommand{\l}{\ell}  %%% l cursiva para primo
	\newcommand{\p}{\mathfrak{p}} %%% p gotica que representa ideal primo
	\newcommand{\q}{\mathfrak{q}} %%% q gotica que representa ideal primo
	\newcommand{\m}{\mathfrak{m}} %%% m gotica que representa ideal maximal
	\newcommand{\st}{\overset{\sim}{\sigma}} %% "sigma tilde"
	\newcommand{\End}{\mathrm{End}}  %% Endomorfismos
	\newcommand{\Aut}{\mathrm{Aut}}  %% Endomorfismos
	\newcommand{\Gal}{\mathrm{Gal}} %%% grupo de galois

	\newcommand{\Mod}[1]{\;\;(\mathrm{mod}\; #1)}
	\newcommand{\leg}[2]{\left(\frac{#1}{#2}\right)}  %% simbolo de legendre
	\newcommand{\wh}[1]{\widehat{#1}} %% abreviacion de \widehat
	\newcommand{\ol}[1]{\overline{#1}} %% abreviacion de \overline
        \newcommand{\mat}[4]{\begin{pmatrix}#1 & #2\\ #3 & #4\end{pmatrix}}  %% matriz de 2x2
        \newcommand{\matt}[4]{\scriptsize \begin{pmatrix}#1 & #2\\ #3 & #4\end{pmatrix}\normalsize}
        %% matriz chica de 2x2

% Abbreviations
	\newcommand{\GQ}{G_{\QQ}} %%% grupo absoluto de Galois de Q
  \newcommand{\Hom}{\mathrm{Hom}} %%% Hom
	\newcommand{\rhot}{\bar{\rho}_{E,3}} %%% representation associated to 3-torsion points of E
	\newcommand{\rhoc}{\bar{\rho}_{E,5}} %%% representation associated to 5-torsion points of E
	\newcommand{\rhop}{\bar{\rho}_{E,p}} %%% representation associated to p-torsion points of E
	\newcommand{\rhoN}{\bar{\rho}_{E,N}} %%% representation associated to N-torsion points of E
  \newcommand{\act}{\curvearrowright}  %%% flecha de accion
  \newcommand{\gag}{\Gamma\alpha\Gamma'} %%% clase bilateral de dos subgrupos de congruencia
  \newcommand{\ord}{\mathrm{ord}} %%% orden

  \newcommand{\Sk}{S_k(\Gamma_0(N))} %%% cuspforms Gamma_0(N) peso k
  \newcommand{\Snew}{S_k^{\mathrm{new}}(\Gamma_0(N))} %%% newforms Gamma_0(N) peso k
  \newcommand{\Snewx}{S_k^{\mathrm{new}}(\Gamma_0(N),\chi)} %%% newforms Gamma_0(N) peso k chi
  \newcommand{\Sold}{S_k^{\mathrm{old}}(\Gamma_0(N))} %%% oldforms Gamma_0(N) peso k
  \newcommand{\Soldx}{S_k^{\mathrm{new}}(\Gamma_0(N),\chi)} %%% oldforms Gamma_0(N) peso k chi
% Operators


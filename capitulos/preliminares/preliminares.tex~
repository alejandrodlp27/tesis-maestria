\documentclass[../../tesis_maestria]{subfiles}
\begin{document}
\section{Formas Modulares y Series de Eisenstein}

Una serie de ejemplos de formas modulares est\'an dadas por las series de Eisenstein. Hay varios
estilos de series de Eisenstein, el m\'as sencillo se define como
\[
  E_{2k}(z):=\frac{1}{2}\sum_{n,m\in\ZZ}\frac{1}{(mz+n)^{2k}} \qquad (k\geq 2).
\]
Claro est\'a que no incluimos el sumando asociado a $n=m=0$; no denotamos esto expl\'icitamente,
pero s\'i lo asumimos impl\'icitamente de ahora en adelante. Ahora, si
\[
  \gamma=
  \begin{pmatrix}
    a & b\\ c&d
  \end{pmatrix} \in \SL_2\ZZ
\]
entonces la funci\'on inducida
\[
  \ZZ\times\ZZ \lra \ZZ\times\ZZ \quad\text{definido por}\quad
  (m,n)\mapsto \gamma^t(m,n)=(am+cn,bm+dn)
\]
es una biyecci\'on (pues $(m,n)\mapsto (\gamma^t)^{-1}(m,n)$ es su inverso). En particular, como
$(0,0)\mapsto(0,0)$, la funci\'on anterior permuta los elementos de $\ZZ\times\ZZ-\{(0,0)\}$.
Por lo tanto:
\begin{align}
  E_{2k}\paren{\frac{az+b}{cz+d}}&=
  \frac{1}{2}\sum_{n,m\in\ZZ}\frac{1}{(m\frac{az+b}{cz+d}+n)^{2k}}=
  \frac{1}{2}\sum_{n,m\in\ZZ}\frac{(cz+d)^{2k}}{(maz+mb+ncz+d)^{2k}}\nonumber\\ & =
  \frac{(cz+d)^{2k}}{2}\sum_{n,m\in\ZZ}\frac{1}{((ma+nc)z+(mb+nd))^{2k}}=
  \frac{(cz+d)^{2k}}{2}\sum_{n,m\in\ZZ}\frac{1}{(mz+n)^{2k}}\nonumber\\ & =
  (cz+d)^{2k}E_{2k}(z) \label{eq:eisenstein2k}.              
\end{align}
Para justificar la permutaci\'on de los sumandos, debemos probar que la serie definida por
$E_{2k}$ es absolutamente convergente. Para esto  sean $\omega_1,\omega_2\in\CC^*$ tales que
$\tfrac{\omega_1}{\omega_2}=z$, entonces $L:=\ZZ\omega_1\oplus\ZZ\omega_2\subset\CC$ es una
ret\'icula, ie. $\{\omega_1,\omega_2\}$ es una $\RR$-base de $\CC$ (esto sucede porque
$\Im(\omega_1/\omega_2)=\Im(z)>0$ implica que $\omega_1$ y $\omega_2$ no son colineales).
De esta manera, si $\sigma\in\RR$:
\[
  \sum_{n,m\in\ZZ}\frac{1}{\abs{mz+n}^{\sigma}}=
  \sum_{n,m\in\ZZ}\frac{\omega_2^{\sigma}}{\abs{m\omega_1+n\omega_2}^{\sigma}}=
  \omega_2^{\sigma}\sum_{\lambda\in L}\frac{1}{\abs{\lambda}^{\sigma}},
\]
otra vez estamos asumiendo impl\'icitamente que el sumando $\lambda=0$ queda excluido de la
suma por razones obvias. Por lo tanto la convergencia absoluta de la serie $E_{2k}$ se reduce
a probar la convergencia del lado derecho para cualquier ret\'icula $L$.

Hay varias maneras de probar que la serie $\sum\abs{\lambda}^{-\sigma}$
converge si y s\'olo si $\sigma>2$ (por ejemplo \cite[\S VII.2.2]{SerreACIA} y
\cite[\S III.8]{ShimuraMFBAB}), pero damos una prueba elemental y visual:

\begin{lema}\label{lema:conv_abs_sobre_reticula} %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% LEMA
  Sea $L=\ZZ\omega_1\oplus\ZZ\omega_2\subset\CC$ una ret\'icula, entonces:
  \[
    \sum_{\lambda\in L}\frac{1}{\abs{\lambda}^{\sigma}}\;\;\text{converge}\quad\iff\quad
    \sigma>2
  \]
  
\end{lema}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{proof}
Todos los elementos de $L-\{0\}$ est\'an sobre un paralelogramo con v\'ertices
$\{n\omega_1+n\omega_2,-n\omega_1+n\omega_2,-n\omega_1-n\omega_2,n\omega_1-n\omega_2\}$ para
alguna $n\geq 1$; a estos paralelogramos los denotamos por $P_n$. Observa que los puntos
de la ret\'icula sobre un lado de $P_n$ son $\{n\omega_1+k\omega_2\mid -n\leq k\leq n\}$.
En particular hay $2n+1$ puntos de la ret\'icula sobre un (y cualquier) lado del paralelogramo.
Por lo tanto hay $8n=4(2n+1)-4$ puntos en $L\cap P_n$ porque los cuatro v\'ertices se cuentan
dos veces. Adem\'as, la homotecia $z\mapsto nz$ lleva los v\'ertices $\pm m\omega_1\pm m\omega_2$ del
paralelogramo $P_m$ a los v\'ertices $\pm nm\omega_1\pm nm\omega_2$ del paralelogramo $P_{nm}$.
Es decir que $n P_m=P_{nm}$.

Ahora tomamos $r>0$ como la distancia m\'inima del $0\in\CC$ al primer paralelogramo $P_1$ y
$R>0$ como la distancia m\'axima del $0$ a $P_1$, ie. $R=\abs{\omega_1+\omega_2}$:

\begin{figure}[h]%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% FIGURA
  \centering
  \includegraphics[scale=0.4]{reticula}
\end{figure}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\noindent De hecho podemos tomar $r$ como el radio de cualquier c\'irculo contenido en $P_1$
y $R$ como el radio de cualquier c\'irculo que contiene a $P_1$.

Entonces si $\lambda\in L\cap P_1$, tenemos que $r\leq\abs{\lambda}\leq R$. M\'as generalmente,
si $\lambda\in L\cap P_n=L\cap n P_1$ entonces $\lambda=n\lambda'$ con $\lambda'\in P_1$ y as\'i:
\[
  r\leq \abs{\lambda'}\leq R \quad\then\quad nr\leq\abs{\lambda}\leq nR
\]
Esta desigualdad implica que
\begin{equation}\label{eq:desigualdad_paralelogramo}
  \frac{1}{n^{\sigma}R^{\sigma}}\leq \frac{1}{\abs{\lambda}^{\sigma}}\leq
  \frac{1}{n^{\sigma}r^{\sigma}} \qquad(\lambda\in P_n\cap L).
\end{equation}

Ahora sumamos esta desigualdad sobre todos las $\lambda\in P_n\cap L$ y obtenemos:
\[
  \frac{8}{n^{\sigma-1}R^{\sigma}}\leq \sum_{\lambda\in P_n\cap L}\frac{1}{\abs{\lambda}^{\sigma}}\leq
  \frac{8}{n^{\sigma-1}r^{\sigma}}
\]
porque $\#(L\cap P_n)=8n$. Por \'ultimo, si ahora sumamos sobre todos los paralelogramos
hasta $P_N$ para alguna $N>0$, obtenemos:
\begin{equation}\label{eq:desigualdad_suma}
  \frac{8}{R^{\sigma}}\sum_{n=1}^N\frac{1}{n^{\sigma-1}}\leq
  \sum_{\underset{i\leq N}{\lambda\in P_i\cap L}}\frac{1}{\abs{\lambda}^{\sigma}}\leq
  \frac{8}{r^{\sigma}}\sum_{n=1}^N\frac{1}{n^{\sigma-1}}.
\end{equation}

Ahora estamos en posici\'on de probar el lema: la cota superior de (\ref{eq:desigualdad_suma})
nos garantiza que si $\sigma>2$, la serie $\sum\abs{\lambda}^{-\sigma}$ converge porque el lado
derecho converge a $8r^{-\sigma}\zeta(\sigma-1)$ porque $\sigma-1>1$. Conversamente, si la serie
$\sum\abs{\lambda}^{-\sigma}$ converge, entonces el lado izquierdo converge a
$8R^{-\sigma}\zeta(\sigma-1)$ lo cual implica que $\sigma-1>1$ y terminamos.
\end{proof}

Con el lema hemos probado que (\ref{eq:eisenstein2k}) es v\'alido y que $E_{2k}$ se transforma
adecuadamente bajo la acci\'on de $\SL_2\ZZ$. Para terminar de probar que $E_{2k}$ es una forma
modular, debemos probar que es holomorfo en $\infty$.

Es bien conocido (por ejemplo \cite[\S1.3, pg. 28]{BumpAFAR}) que $E_{2k}$ tiene la siguiente
expansi\'on en serie de Fouier:
\begin{equation}\label{eq:fourier_eisenstein2k}
  E_{2k}(z)=
  \zeta(2k)+\frac{(-1)^k(2\pi)^{2k}}{(2k-1)!}\sum_{n=1}^{\infty}\sum_{d\mid n}d^{2k-1}e^{2\pi inz}
\end{equation}
Esta f\'ormula claramente prueba que $E_{2k}(z)$ es ana\'itica en $\infty$ porque no tiene
coeficientes negativos de Fourier. No escribimos la prueba de esta identidad porque m\'as adelante
probaremos una f\'ormula m\'as general. Concluimos que las f\'ormulas (\ref{eq:eisenstein2k}) y
(\ref{eq:fourier_eisenstein2k}) implican que $E_{2k}\in M_{2k}(\SL_2\ZZ)$.

En la prueba de la modularidad de $\rhot$, aparece una generalizaci\'on de $E_{2k}$ definida
por
\[
  E_{k,\chi}(z):=\sum_{n,m\in\ZZ}\frac{\chi(m)}{(mz+n)^k}
\]
donde $\chi$ es un caracter de Dirichlet.
El problema con esta serie es que en la demostraci\'on de la modularidad de $\rhot$, necesitamos
que el peso sea $k=1$ y la serie anterior no converge para este valor de $k$. Para poder evadir
este problema, introducimos un factor adicional que depende de un parametro complejo $s$:

\begin{defin}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% DEFINICION
  Sea $k\in\ZZ$ y $\chi:(\ZZ/N\ZZ)^*\ra\CC^*$ un caracter de Dirichlet m\'odulo $N$,
  entonces la \emph{serie de Eisenstein de peso k y caracter $\chi$ y parametro s} se define como
  \[
    E_{k,\chi}(z,s):=\sum_{n,m\in\ZZ}\frac{\chi(m)}{(mz+n)^{k}\abs{mz+n}^{2s}}
  \]
  donde $z\in\HH$ y $s\in\CC$.
\end{defin}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

Por el lema \ref{lema:conv_abs_sobre_reticula} la serie $E_{k,\chi}(z,s)$ es absolutamente
convergente cuando $k+2\Re(s)>2$ o equivalentemente $\Re(s)>1-\frac{k}{2}$ (observa que el factor
$\chi(m)$ no afecta la convergencia absoluta porque $\abs{\chi(m)}=1$). Adem\'as la serie
es uniformemente convergente en cualquier conjunto compacto $K$ en el semiplano $\Re(s)>1-\frac{k}{2}$
porque para cualquier compacto en este semiplano, existe una $\eps>0$ tal que
$\Re(s)>1-\frac{k}{2}+\frac{\eps}{2}$ para toda $s\in K$. De esta manera $k+2\Re(s)>2+\eps$ y
tenemos que:
\[
  \sum_{n,m\in\ZZ}\frac{1}{\abs{mz+n}^{k+2\Re(s)}}\leq
  \sum_{n,m\in\ZZ}\frac{1}{\abs{mz+n}^{2+\eps}}<\infty \qquad(\forall s\in K).
\]
Por lo tanto la serie $E_{k,\chi}(z,s)$ es uniformemente convergente en $s$ sobre cualquier
compacto contenido en el semiplano $\Re(s)>1-\frac{k}{2}$. Por el teorema de Weierstrass, esto
implica que $E_{k,\chi}(z,s)$ define una funci\'on holomorfa en $s$ sobre el semiplano
$\Re(s)>1-\frac{k}{2}$.

Sea
\[
  \gamma=\begin{pmatrix}a & b\\ c&d\end{pmatrix} \in \Gamma_0(N):=
  \left\{\begin{pmatrix}a & b\\ c&d\end{pmatrix}\in\SL_2\ZZ \mid c\equiv 0 \Mod{N}\right\},
\]
entonces:
\begin{align}
  E_{k,\chi}(\gamma z,s)&=
  \sum_{n,m\in\ZZ}
  \frac{\chi(m)}{\paren{m\frac{az+b}{cz+d}+n}^k\abs{m\frac{az+b}{cz+d}+n}^{2s}}\nonumber\\ & =
  \sum_{n,m\in\ZZ}%
  \frac{\chi(m)(cz+d)^k\abs{cz+d}^{2s}}{((ma+nc)z+(mb+nd))^k\abs{(ma+nc)z+(mb+nd)}^{2s}}\nonumber\\ & =
  (cz+d)^k\abs{cz+d}^{2s}\sum_{n,n\in\ZZ}
  \frac{\chi(m)}{((ma+nc)z+(mb+nd))^k\abs{(ma+nc)z+(mb+nd)}^{2s}}.\label{eq:trans_eisenstein}
\end{align}
Ahora, como $\gamma\in\Gamma_0(N)\subset\SL_2\ZZ$ entonces $ad-bc=\pm1$ lo cual implica que
\begin{align*}
\pm1\equiv ad-bc\equiv ad \Mod{N} &\quad\then\quad
\chi(ma+cn)=\chi(ma)=\chi(m)\chi(a)\\ &\quad\then\quad
\chi(m)=\chi(ma+cn)\chi(\mp d)=\chi(ma+cn)\chi(\mp1)\chi(d).
\end{align*}
Sustituimos esta f\'ormula para $\chi(m)$ en (\ref{eq:trans_eisenstein}) y recordamos que
$(m,n)\mapsto(ma+nc,mb+nd)$ permuta los elementos de $\ZZ\times\ZZ$ para concluir que:
\begin{align*}
  E_{k,\chi}(\gamma z,s)&=
  (cz+d)^k\abs{cz+d}^{2s}\chi(\mp1)\chi(d)\sum_{n,n\in\ZZ}
  \frac{\chi(ma+nc)}{((ma+nc)z+(mb+nd))^k\abs{(ma+nc)z+(mb+nd)}^{2s}} \\ & =
  (cz+d)^k\abs{cz+d}^{2s}\chi(\mp1)\chi(d)\sum_{n,m\in\ZZ}\frac{\chi(m)}{(mz+n)^{k}\abs{mz+n}^{2s}}
\end{align*}
Por lo tanto
\begin{equation}\label{eq:trans_eisenstein_gen}
  E_{k,\chi}(\gamma z,s) =(cz+d)^k\abs{cz+d}^{2s}\chi(\mp1)\chi(d) E_{k,\chi}(z,s).
\end{equation}

Si hacemos $s=0$, entonces la f\'ormula anterior implica que $E_{k,\chi}$ se transforma
adecuadamente para ser una forma modular de peso $k$, nivel $N$ y nebentypus $\chi$. El
problema es que la serie definida por $E_{k,\chi}(z,0)$ no converge si $k=1$ que es el
caso que nos interesa para la prueba de la modularidad de $\rhot$. Entonces lo que haremos
es continuar anal\'iticamente $E_{k,\chi}(z,s)$ a $s=0$ y que la continuaci\'on sea holomorfo
en $s=0$. De esta manera obtendremos una forma modular.

En la prueba de la modularidad de $\rhot$, se usa una serie de Eisenstein particular, entonces
a continuaci\'on nos enfocaremos solamente en este caso porque la prueba de este caso se generaliza
sin muchas complicaciones; referimos a \cite[\S7.2]{MiyakeMF} para el caso general. En lo que sigue
vamos a continuar anal\'iticamente $E_{1,\chi}(z,s)$ donde $k=1$, $N=3$ y
\[
  \chi(m)=\leg{m}{3}=
  \begin{cases}
    1 & m\equiv 1 \Mod{3}\\
    -1 & m\equiv -1 \Mod{3}\\
    0 & m\equiv 0 \Mod{3}
  \end{cases}
\]
es el s\'imbolo de Legendre m\'odulo 3 (ie. el \'unico caracter cuadr\'atico m\'odulo 3).
Observemos que $\chi(-1)=-1$ implica que
\[
  \frac{\chi(-m)}{(-mz+n)\abs{-mz+n}^{2s}}=\frac{\chi(m)}{(mz-n)\abs{mz-n}^{2s}}.
\]

Ahora, la serie $E_{1,\chi}(z,s)$ converge absolutamente cuando $\Re(s)>1/2$, entonces en
este caso reordenamos los sumandos:
\begin{align}
  E_{1,\chi}(z,s) & =\sum_{n,m\in\ZZ}\frac{\chi(m)}{(mz+n)\abs{mz+n}^{2s}}\nonumber\\
                 & =\sum_{n=-\infty}^{\infty}\frac{\chi(0)}{n\abs{n}^{2s}}+
                    \sum_{\underset{m\neq0}{m=-\infty}}^{\infty}\sum_{n=-\infty}^{\infty}
                   \frac{\chi(m)}{(mz+n)\abs{mz+n}^{2s}}\nonumber\\
                  & =\sum_{m=1}^{\infty}\sum_{n=-\infty}^{\infty}\frac{\chi(m)}{(mz+n)\abs{mz+n}^{2s}}+
                    \frac{\chi(-m)}{(-mz+n)\abs{-mz+n}^{2s}}\nonumber\\
                  & =\sum_{m=1}^{\infty}\chi(m)\sum_{n=-\infty}^{\infty}\frac{1}{(mz+n)\abs{mz+n}^{2s}}+
                    \frac{1}{(mz-n)\abs{mz-n}^{2s}}\nonumber\\
  \therefore E_{1,\chi}(z,s)& = 2\sum_{m=1}^{\infty}\chi(m)\sum_{n=-\infty}^{\infty}
                    \frac{1}{(mz+n)\abs{mz+n}^{2s}} \qquad (\Re(s)>\tfrac{1}{2})\label{eq:primer_E1chi}
\end{align}
Podemos reescribir la serie $\sum(mz+n)^{-1}\abs{mz+n}^{-2s}$ en otra serie m\'as sencilla. Primero
observemos que:
\begin{align*}
  (mz+n)^{-1}\abs{mz+n}^{-2s}&=(mz+n)^{-1}(mz+n)^{-s}(\overline{mz+n})^{-s}
                             =(mz+n)^{-1-s}(m\bar{z}+n)^{-s}\\
                            &=m^{-1-2s}\paren{z+\tfrac{n}{m}}^{-1-s}\paren{\bar{z}+\tfrac{n}{m}}^{-s}.
\end{align*}
Ahora fijamos un $m$ y dividimos $\ZZ$ en las $m$ clases laterales $0+m\ZZ,1+m\ZZ,\ldots,(m-1)+m\ZZ$.
De esta manera, si $n\in r+m\ZZ$, entonces $n=n'm+r$ para alguna $n'\in\ZZ$ y as\'i:
\[
  (mz+n)^{-1}\abs{mz+n}^{-2s}=
  m^{-1-2s}\paren{z+\tfrac{r}{m}+n'}^{-1-s}\paren{\bar{z}+\tfrac{r}{m}+n'}^{-s}
\]
Por lo tanto:
\begin{align*}
\sum_{n=-\infty}^{\infty}\frac{1}{(mz+n)\abs{mz+n}^{2s}} &=
\sum_{r=0}^{m-1}\sum_{n'\in\ZZ}
\frac{m^{1+2s}}{\paren{z+\tfrac{r}{m}+n'}^{1+s}\paren{\bar{z}+\tfrac{r}{m}+n'}^s}
\end{align*}

Parece que hemos complicado las cuentas, pero en realidad la segunda serie del lado derecho
es un ejemplo de una funci\'on cuyas propiedades son conocidas: siguiendo a \cite[\S7.2]{MiyakeMF}
definimos
\[
  S(z;\alpha,\beta):=\sum_{n=-\infty}^{\infty}(z+n)^{-\alpha}(\bar{z}+n)^{-\beta}
\]
donde $z\in\HH$ y $\alpha,\beta\in\CC$. Si sustituimos la notaci\'on $S(z;\alpha,\beta)$ en
(\ref{eq:primer_E1chi}) obtenemos
\begin{equation}\label{eq:E_en_terminos_de_S}
  E_{1,\chi}(z,s)=2\sum_{m=1}^{\infty}\chi(m)m^{-1-2s}\sum_{r=0}^{m-1}S\paren{z+\tfrac{r}{m};1+s,s}.
\end{equation}
Observa que si $\Re(\alpha)+\Re(\beta)>1$ entonces la serie $S(z;\alpha,\beta)$ converge
absolutamente porque
\[
  \abs{z+n}=\abs{\overline{z+n}}=\abs{\bar{z}+n}=\sqrt{(\Re(z)+n)^2+\Im(z)^2}=\Oo(n),
\]
y as\'i
\begin{equation}\label{eq:orden_sumandos_S}
  \abs{(z+n)^{-\alpha}(\bar{z}+n)^{-\beta}}=\abs{z+n}^{-\Re(\alpha)}\abs{\bar{z}+n}^{-\Re(\beta)}=
  \Oo\paren{n^{-\Re(\alpha+\beta)}}.
\end{equation}

La ventaja de usar la serie $S(z;\alpha,\beta)$ es que se puede aplicar la f\'ormula de sumaci\'on
de Poisson (FSP) para calcular esta serie. M\'as precisamente si escribimos $z=x+iy$ con $y>0$ y
si denotamos
\[
  \phi(x,y;\alpha,\beta):=(x+iy)^{-\alpha}(x-iy)^{-\beta},
\]
entonces tenemos que
\[
  S(x+iy;\alpha,\beta)=\sum_{n=-\infty}^{\infty}\phi(x+n,y;\alpha,\beta).
\]
Observa que (\ref{eq:orden_sumandos_S}) implica que, como funci\'on de $x$ tenemos:
\[
  \abs{\phi(x,y;\alpha,\beta)}=\Oo(x^{-\Re(\alpha+\beta)})
\]
lo cual implica que si $\Re(\alpha+\beta)>1$, entonces $\phi$ es absolutamente integrable sobre
$\RR$, ie. $\phi(x,y;\alpha,\beta)\in L^1(\RR)$ como funci\'on de $x$. Por lo tanto $\phi$ tiene
una transformada de Fourier:
\[
  \hat{\phi}(u,y;\alpha,\beta)=\int_{-\infty}^{\infty}\phi(x,y;\alpha,\beta)e^{-2\pi iux}dx
  \qquad (y>0,\Re(\alpha,\beta)>1).
\]

Ahora, queremos aplicar la f\'ormula de sumaci\'on de Poisson para concluir que
\begin{equation}\label{eq:suma_poisson}
  S(x+iy;\alpha,\beta)=\sum_{n=-\infty}^{\infty}\hat{\phi}(n,y;\alpha,\beta)e^{2\pi inx}.
\end{equation}

Observamos que
\[
  \abs{(x+iy)^{-\alpha}(x-iy)^{-\beta}}=\abs{x+iy}^{-\Re(\alpha+\beta)}\leq \abs{y}^{-\Re(\alpha+\beta)}
\]
implica que la serie $\sum \abs{\phi(x+n,y;\alpha,\beta)}$ converge uniformemente en la variable
$x$ cuando $\Re(\alpha+\beta)>1$. Este hecho es  un paso importante en la prueba de la FSP.
En efecto, durante la prueba se define una nueva funci\'on $F(x):=\sum \phi(x+n,y;\alpha,\beta)$ que
resulta ser peri\'odica con periodo 1 y para poder verificar que $F$ coincide con su serie de
Fourier (y as\'i deducir la FSP), es suficiente ver que $F$ es continua. La convergencia uniforme
de la serie $\sum \phi(x+n,y;\alpha,\beta)$ implica  la continuidad de $F$.

Por \'ultimo, es necesario probar que el lado derecho de (\ref{eq:suma_poisson}) es absolutamente
convergente para poder justificar la igualdad en (\ref{eq:suma_poisson}). En general tenemos:

\begin{thm}\label{thm:S_holomorfo_gen}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% TEOREMA
  La funci\'on $S(z;\alpha,\beta)$ tiene continuaci\'on anal\'itica a $\CC\times\CC$ con respecto
  de $(\alpha,\beta)$ a una funci\'on meromorfa y tiene f\'ormula:
  \[
    S(z;\alpha,\beta)=\hat{\phi}(0,y;\alpha,\beta)+
    \sum_{\underset{n\neq0}{n=-\infty}}^{\infty}\hat{\phi}(n,y;\alpha,\beta)e^{2\pi inx}
    \qquad (z=x+iy\in\HH).
  \]
  Adem\'as la serie del lado derecho converge uniformemente y absolutamente sobre cualquier
  conjunto compacto de $\HH\times\CC\times\CC$ y $\Gamma(\alpha+\beta-1)^{-1}S(z;\alpha,\beta)$
  es una funci\'on entera (donde $\Gamma$ denota la funci\'on gamma).
\end{thm}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

A nosotros nos interesa el caso $\alpha=s$ y $\beta=s+1$, entonces redefinimos la funci\'on:
\[
  \phi(x,y;s):=\phi(x,y;s,s+1)=(x+iy)^{-s}(x-iy)^{-(s+1)}
\]
donde similarmente $\abs{\phi(x,y;s)}=\Oo(x^{2\Re(s)-1})$ lo cual implica que $\phi(x,y;s)$
es absolutamente integrable como funci\'on de $x$ cuando $\Re(s)>\tfrac{1}{2}$ (que es la misma
condici\'on que pedimos para la convergencia absoluta de la serie $E_{1,\chi}(z;s)$). De esta
manera $\phi$ tiene una transformada de Fourier:
\begin{equation}\label{eq:trans_de_phi_uno}
  \hat{\phi}(u,y;s)=\int_{-\infty}^{\infty}\phi(x,y;s)e^{-2\pi iux}dx
  =\int_{-\infty}^{\infty}\frac{e^{-2\pi iux}dx}{(x+iy)^{s}(x-iy)^{s+1}}.
\end{equation}
Con esta notaci\'on tambi\'en tenemos que
\[
  S(x+iy;s):=S(x+iy;s,s+1)=
  \sum_{n=-\infty}^{\infty}\phi(x+n,y;s).
\]
Con esto probamos una nuestra versi\'on del teorema \ref{thm:S_holomorfo_gen}. Nada m\'as
mencionamos que la prueba que damos a continuaci\'on contiene todo lo necesario para generalizarse
sin muchos problemas al teorema \ref{thm:S_holomorfo_gen}; referimos de nuevo a
\cite[Teorema 7.2.8]{MiyakeMF} para el caso general.

\begin{thm}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%TEOREMA
  Para $\Re(s)>$
\end{thm}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\begin{proof}
  Primero calculamos $\hat{\phi}$. Para resolver la integral (\ref{eq:trans_de_phi_uno}), hay que
  expresar los dos factores del denominador,como transformadas de Fourier de otras. M\'as
  precisamente, definimos $f(x):=(y+ix)^{-s-1)}$ y define
  \[
    g(w):=
    \begin{cases}
      e^{-yw}w^{s} &(0<w)\\
      0 &(w\leq 0)
    \end{cases}
  \]
donde $g(w)\in L^1(\RR)$ porque $y>0$ y $\Re(s)>\tfrac{1}{2}$. Si calculamos su transformada de
Fourier, obtenemos:
\begin{gather}
  \hat{g}\paren{\frac{x}{2\pi}}=
  \int_{-\infty}^{\infty}e^{-iwx}g(w)dw=
  \int_0^{\infty}e^{-(y+ix)w}w^sdw=
  (y+ix)^{-s-1}\Gamma(s+1)\nonumber\\
  \therefore \;\; f(x)=\frac{1}{\Gamma(s+1)} \hat{g}\paren{\frac{x}{2\pi}}
  \label{eq:trans_de_g_dos}
\end{gather}
porque la funci\'on gamma cumple
\begin{gather}
  \int_0^{\infty}e^{-zt}t^{v-1}dt=
  \int_0^{\infty}e^{-z'}\paren{\tfrac{z'}{z}}^{v-1}\frac{dz'}{z}=
  z^{-v}\int_0^{\infty}e^{-z'}(z')^{v-1}dz'\nonumber\\
  \therefore\;\;z^{-v}\Gamma(v) = \int_0^{\infty}e^{-zt}t^{v-1}dt
  \qquad(\Re(z),\Re(v)>0)\label{eq:prop_gamma}
\end{gather}
(por ejemplo \cite[\S5.2.5]{AhlforsCA}). En general y de manera similar, tenemos:
\begin{equation}\label{eq:denominador}
  \frac{1}{(y\pm ix)^s}=
  \frac{1}{\Gamma(s)}\int_0^{\infty}e^{-(y\pm ix)w}w^{s-1}dw\qquad(\Re(s)>0)
\end{equation}
Si aplicamos la transformada de Fourier a (\ref{eq:trans_de_g_dos}) obtenemos
\begin{equation}\label{eq:trans_de_f}
  \frac{2\pi}{\Gamma(s+1)}g\paren{-2\pi w}=\hat{f}(w):=
  \int_{-\infty}^{\infty}\frac{e^{-2\pi iwx}dx}{(y+ix)^{s+1}}
\end{equation}
donde hemos usado la f\'ormula de inversi\'on de Fourier, ie. $\hat{\hat{g}}(x)=g(-x)$ y como se
modifica la transformada de Fourier bajo homotecias, ie.
$\widehat{g(\delta u)}(x)=\delta^{-1}g(\delta^{-1}x)$ (por ejemplo (cita)).%\cite[\S]{})

Queremos usar (\ref{eq:trans_de_f}) para calcular (\ref{eq:trans_de_phi_uno}), pero hay
que hacer dos cosas antes de poderlo hacer. Primero debemos intercambiar los roles de $x$ y $y$
multiplicando por $i$, m\'as precisamente como
\[
  (x+iy)^s(x-iy)^{s+1}=
  (i(y-ix))^s(-i(y+ix))^{s+1}\overset{*}{=}
  -i(y-ix)^s(y+ix)^{s+1},
\]
donde (*) es v\'alido porque $\Re(y\pm ix)>0$, entonces:
\begin{equation}\label{eq:trans_de_phi}
  \hat{\phi}(u,y;s)=
  i\int_{-\infty}^{\infty}\frac{e^{-2\pi iux}dx}{(y-ix)^s(y+ix)^{s+1}}.
\end{equation}

Desp\'ues debemos aislar un factor del denominador para poder sustituir (\ref{eq:trans_de_f})
en (\ref{eq:trans_de_phi}). Para esto, sustituimos la f\'ormula (\ref{eq:denominador}) en
(\ref{eq:trans_de_phi}) y queda:
\[
  \hat{\phi}(u,y;s)=
  \frac{i}{\Gamma(s)}\int_{-\infty}^{\infty}\frac{e^{-2\pi iux}}{(y+ix)^{s+1}}
  \paren{\int_0^{\infty}e^{-(y-ix)w}w^{s-1}dw}dx.
\]
Como el valor absoluto del integrando es $e^{-yw}w^{\Re(s)-1}\abs{x+iy}^{-\Re(s)-1}$,
la integral anterior es absolutamente integrable cuando $\Re(s)>0$ o en particular cuando
$\Re(s)>\tfrac{1}{2}$). En este caso podemos intercambiar el orden de integraci\'on
(Teorema de Fubini) para obtener:
\begin{align}
  \hat{\phi}(u,y;s)&=
  \frac{i}{\Gamma(s)}\int_{0}^{\infty}w^{s-1}e^{-yw}
  \paren{\int_{-\infty}^{\infty}
  \frac{e^{-2\pi ix\paren{u-\tfrac{w}{2\pi}}}}{(y+ix)^{s+1}}dx}dw\label{eq:intercambio_int}\\
  &\overset{*}{=}\frac{i}{\Gamma(s)}\int_{0}^{\infty}w^{s-1}e^{-yw}
                                \paren{\frac{2\pi}{\Gamma(s+1)}g\paren{-2\pi u+w}}dw.\nonumber
\end{align}
donde la igualdad (*) se sigue de la f\'ormula (\ref{eq:trans_de_f}). Por la definici\'on
de $g$ tenemos que $g(w-2\pi u)\neq0$ cuando $w>2\pi u$ y as\'i tenemos:
\[
  \hat{\phi}(u,y;s)=
  \frac{2\pi i}{\Gamma(s)\Gamma(s+1)}
  \int_{2\pi u}^{\infty}e^{-2y(w-\pi u)}w^{s-1}(w-2\pi u)^sdw.
\]
Hacemos el cambio de variable $w-\pi u\mapsto w$ para obtener
\[
  \hat{\phi}(u,y;s)=
  \frac{2\pi i}{\Gamma(s)\Gamma(s+1)}
  \int_{\pi u}^{\infty}e^{-2yw}(w+\pi u)^{s-1}(w-\pi u)^sdw
  \qquad(\Re(s)>0).
\]
Calcular el caso $u=0$ es simplemente  una aplicaci\'on de la f\'ormula integral de
la funci\'on gamma (cf. ecuaci\'on \ref{eq:prop_gamma}):
\[
  \hat{\phi}(0,y;s)=\frac{2\pi i}{\Gamma(s)\Gamma(s+1)}\int_0^{\infty}e^{-2yw}w^{s-1}w^sdw=
  \frac{2\pi i\Gamma(2s)}{(2y)^{2s}\Gamma(s)\Gamma(s+1)}.
\]

Si $u>0$ entonces hacemos el cambio de variable $w\mapsto\pi u w$ seguido del cambio
$w\mapsto w+1$ y por \'ultimo hacemos $w\mapsto2w$:
\begin{align}
  \hat{\phi}(u,y;s)&=
  \frac{2\pi i}{\Gamma(s)\Gamma(s+1)}
  \int_1^{\infty}e^{-2\pi yuw}(\pi wu+\pi u)^{s-1}(\pi wu-\pi u)^s(\pi u)dw\nonumber\\
  &=\frac{2\pi i(\pi u)^{2s}}{\Gamma(s)\Gamma(s+1)}
  \int_1^{\infty}e^{-2\pi yuw}(w+1)^{s-1}(w-1)^sdw\nonumber\\
  &=\frac{2\pi i(\pi u)^{2s}e^{-2\pi yu}}{\Gamma(s)\Gamma(s+1)}
    \int_0^{\infty}e^{-2\pi yuw}(w+2)^{s-1}w^sdw\nonumber\\
  &=\frac{2\pi i(2\pi u)^{2s}e^{-2\pi yu}}{\Gamma(s)\Gamma(s+1)}
    \int_0^{\infty}e^{-4\pi yuw}(w+1)^{s-1}w^sdw\qquad(u>0)\label{eq:u_posi}
\end{align}
La integral es  la \emph{funci\'on hipergeom\'etrica confluente}%
\footnote{En realidad la funci\'on hipergeom\'etrica confluente la defini\'o originalmente
  Kummer en 1836 como una serie que resuelve la ecuaci\'on diferencial de Kummer \cite{Kummer1836}.
  La f\'ormula integral de esta funci\'on apareci\'o despu\'es.}
definida por:
\[
  \sigma(z;\alpha,\beta)=\int_0^{\infty}e^{-zw}(w+1)^{\alpha-1}w^{\beta-1}dw
  \qquad(\Re(z),\Re(\beta)>0,\alpha\in\CC),
\]
m\'as precisamente:
\[
  \hat{\phi}(u,y;s)=
  \frac{2\pi i(2\pi u)^{2s}e^{-2\pi yu}}{\Gamma(s)\Gamma(s+1)}\sigma(4\pi yu;s,s+1).
\]

Ahora, si $u<0$, para calcular $\hat{\phi}(u,y;s)$ hacemos el cambio de variable
$w\mapsto\pi \abs{u} w$ seguido del cambio $w\mapsto w+1$ y despu\'es $w\mapsto2w$:
\begin{align}
  \hat{\phi}(u,y;s)&=
  \frac{2\pi i}{\Gamma(s)\Gamma(s+1)}
  \int_1^{\infty}e^{-2\pi y\abs{u}w}(\pi w\abs{u}+\pi u)^{s-1}(\pi w\abs{u}-\pi u)^s(\pi \abs{u})dw
  \nonumber\\
  &=\frac{2\pi i \pi^{2s}\abs{u}^{2s}}{\Gamma(s)\Gamma(s+1)}
  \int_1^{\infty}e^{-2\pi y\abs{u}w}(w-1)^{s-1}(w+1)^sdw\nonumber\\
  &=\frac{2\pi i(\pi u)^{2s}e^{-2\pi y\abs{u}}}{\Gamma(s)\Gamma(s+1)}
    \int_0^{\infty}e^{-2\pi y\abs{u}w}(w+2)^sw^{s-1}dw\nonumber\\
  &=\frac{2\pi i(2\pi u)^{2s}e^{-2\pi y\abs{u}}}{\Gamma(s)\Gamma(s+1)}
  \int_0^{\infty}e^{-4\pi y\abs{u}w}(w+1)^sw^{s-1}dw\qquad(u<0)\nonumber\\
  &=\frac{2\pi i(2\pi u)^{2s}e^{-2\pi y\abs{u}}}{\Gamma(s)\Gamma(s+1)}\sigma(4\pi y\abs{u};s+1,s).
\end{align}
Nos queda una expresi\'on muy similar al caso $u>0$ (ecuaci\'on \ref{eq:u_posi}).

La ventaja de escribir $\hat{\phi}$ en t\'erminos de $\sigma$ es que tenemos:
\begin{lema}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% LEMA
  La integral $\sigma(z;\alpha,\beta)$ es uniformemente convergente sobre cualquier subconjunto
  compacto de $\HH'\times\CC\times\HH'$. Adem\'as, como funci\'on, $\sigma(z;\alpha,\beta)$ se
  puede continuar anal\'iticamente a una funci\'on meromorfa definido sobre $\HH'\times\CC\times\CC$
  que cumple que, para todo subconjunto compacto $Q$ de $\CC\times\CC$ existen constantes $A,B>0$
  tales que:
  \[
    \abs{\sigma(y;\alpha,\beta)} \leq A(1+y^{-B}) \qquad\forall (\alpha,\beta)\in Q\;\;,\;\; y>0.
  \]
\end{lema}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{proof}(lema)
  Observa que el integrando de $\sigma(z;\alpha,\beta)$ es de orden $w^{\Re(\beta)-1}$ cuando
$w\ra0$ porque 
\[
  \lim_{w\ra0}\abs{e^{-zw}(w+1)^{\alpha-1}w^{\beta-1}}=\lim_{w\ra0}w^{\Re(\beta)-1}\quad\then\quad
  \abs{e^{-zw}(w+1)^{\alpha-1}w^{\beta-1}}=\Oo(w^{\Re(\beta)-1})\quad(w\ra0)
\]
y es de orden $e^{-\Re(z)w}$ cuando $w\rai$ porque
\[
  \lim_{w\rai}\frac{(w+1)^{\Re(\alpha)-1}w^{\Re(\beta)-1}}{e^{\Re(z)w}}=0 \quad\then\quad
  \abs{e^{-zw}(w+1)^{\alpha-1}w^{\beta-1}}=\Oo(e^{-\Re(z)w})\quad(w\rai).
\]

Por lo tanto la integral es convergente cuando $\Re(z)>0$ y $\Re(\beta)>0$; observa que no hay
restricci\'on para $\alpha$.

Adem\'as sea $Q'$ cualquier subconjunto compacto de $\HH'\times\CC\times\HH'$ y sean $\eps>0$ y
$\delta>0$ tales que $\Re(z)>\eps$ y $\Re(\beta)>\delta$ para toda $z\in Q\cap\HH'$ y para toda
$\beta\in Q\cap\HH'$. Entonces nuestras aproximaciones del integrando implican que existen
constantes $C_1,C_2>0$ tales que:
\[
  \abs{e^{-zw}(w+1)^{\alpha-1}w^{\beta-1}}=e^{-\Re(z)w}(w+1)^{\Re(\alpha)-1}w^{\Re(\beta)-1}
  \begin{cases}
    \leq C_1e^{-\eps w} &\text{para}\;w\;\text{suficientemente grande}\\
    \leq C_2w^{\delta-1} &\text{para}\;w\;\text{suficientemente peque\~na}
  \end{cases}
\]
Por lo tanto la integral $\sigma(z;\alpha,\beta)$ converge uniformemente sobre cualquier
subconjunto compato de $\HH'\times\CC\times\HH'$.

Para continuar $\sigma(z;\alpha,\beta)$ anal\'iticamente, primero asumimos que $z>0$ y que
$\Re(\alpha)>0$ (adem\'as de que $\Re(\beta)>0$ donde $\sigma(z;\alpha,\beta)$ es convergente).
Entonces:
\begin{align*}
  \sigma(z;1-\beta,\alpha)
  &\;=\;\int_0^{\infty}e^{-zw}(w+1)^{-\beta}w^{\alpha-1}dw\\
  &\overset{(\ref{eq:prop_gamma})}{=}
    \int_0^{\infty}
    e^{-zw}\paren{\frac{1}{\Gamma(\beta)}\int_0^{\infty}e^{-(w+1)v}v^{\beta-1}dv}w^{\alpha-1}dw\\
  &\;=\;\frac{1}{\Gamma(\beta)}\int_0^{\infty}
    e^{-v}v^{\beta-1}\paren{\int_0^{\infty}e^{-(z+v)w}w^{\alpha-1}dw}dv\\
  &\overset{(\ref{eq:prop_gamma})}{=}
    \frac{\Gamma(\alpha)}{\Gamma(\beta)}\int_0^{\infty}
    e^{-v}v^{\beta-1}(z+v)^{-\alpha}dv\\
  &\;=\;\frac{\Gamma(\alpha)}{\Gamma(\beta)}z^{\beta-\alpha}\int_0^{\infty}
    e^{-v}v^{\beta-1}(v+1)^{-\alpha}dv\\
  \therefore\quad \Gamma(\alpha)^{-1}z^{\alpha}\sigma(z;1-\beta,\alpha)
  &\;=\;\Gamma(\beta)^{-1}z^{\beta}\sigma(z;1-\alpha,\beta)
    \qquad\forall (z,\alpha,\beta)\in\RR^+\times\HH'\times\HH',
\end{align*}
donde el intercambio de las integrales se verifica de exactamente la misma manera que el
intercambio anterior (\ref{eq:intercambio_int}).

La \'ultima igualdad de la ecuaci\'on anterior nos motiva a cambiar $\sigma$ por otra funci\'on
donde la simetr\'ia se vea m\'as sencilla. Con
\[
  \st(z;\alpha,\beta):=\Gamma(\beta)^{-1}z^{\beta}\sigma(z;\alpha,\beta).
\]
tenemos
\[
  \st(z;1-\beta,\alpha)=\st(z;1-\alpha,\beta).
\]
Gracias a la primera afirmaci\'on del lema, $\sigma(z;\alpha,\beta)$ es una funci\'on
holomorfa sobre $\HH'\times\HH'\times\HH'$ (recuerda que la funci\'on gamma nunca se anula).
Como $\Gamma(\beta)^{-1}z^{\beta}$ tambi\'en es una funci\'on holomorfa sobre el mismo dominio,
entonces $\st$ es holomorfa sobre $\HH'\times\HH'\times\HH'$ . Por lo tanto coinciden en
esa regi\'on. La simetr\'ia que exhibe $\st$ se puede reescribir como
\begin{equation}\label{eq:sim_st}
  \st(z;\alpha,\beta)=\st(z;1-\beta,1-\alpha)\qquad (\Re(z)>0,\Re(\alpha)<1,\Re(\beta)>0).
\end{equation}

Ahora extendemos la regi\'on de definici\'on de $\st$ de la siguiente manera:
\begin{equation}\label{eq:semi_ext_st}
  \st(z;\alpha,\beta)=
  \begin{cases}
    \st(z;\alpha,\beta) & \alpha\in\CC,\Re(\beta)>0\\
    \st(z;1-\beta,1-\alpha) & \Re(\alpha)<1,\beta\in\CC
  \end{cases}
\end{equation}
Ahora solamente nos falta ver c\'omo definir $\st(z;\alpha,\beta)$ cuando $\Re(z)>0$,
$\Re(\alpha)\geq1$ y $\beta\in\CC$. Resulta que derivar $\st$ con respecto de $z$ disminuye
la parte real de $\alpha$ por 1, y la idea es derivar suficientes veces hasta que $\Re(\alpha)<1$
para poder reducir al caso anterior.

M\'as precisamente, sabemos que la integral $\sigma(z;\alpha,\beta)$ es uniformemente
convergente en la variable $z$ sobre subconjuntos compactos de $\HH'$, cuando $\Re(\alpha)>0$ y
$\Re(\beta)>0$. Entonces podemos intercambiar la derivada con la integral:
\begin{align*}
  \frac{\partial}{\partial z}\paren{e^{-z}\sigma(z;\alpha,\beta)}
  &=\frac{\partial}{\partial z}\paren{\int_0^{\infty}e^{-zw-z}(w+1)^{\alpha-1}w^{\beta-1}dw}\\
  &=\int_0^{\infty}(w+1)^{\alpha-1}w^{\beta-1}\frac{\partial}{\partial z}e^{-z(w+1)}dw\\
  &=\int_0^{\infty}(w+1)^{\alpha-1}w^{\beta-1}(-1)(w+1)e^{-z(w+1)}dw\\
  &=-e^{-z}\int_0^{\infty}e^{-zw}(w+1)^{\alpha}w^{\beta-1}dw\\
  &=-e^{-z}\sigma(z;\alpha+1,\beta) \qquad \forall (z,\alpha,\beta)\in\HH'\times\HH'\times\HH'
\end{align*}
Por inducci\'on tenemos que:
\begin{equation}
  \label{eq:n_derivada_st}
  \frac{\partial^n}{\partial z^n}\paren{e^{-z}\sigma(z;\alpha,\beta)}=
  (-1)^ne^{-z}\sigma(z;\alpha+n,\beta).
\end{equation}

Con esta f\'ormula calculamos la $n$-\'esima derivada de $\st$ para obtener:
\begin{align*}
  \frac{\partial^n}{\partial z^n}\paren{e^{-z}z^{-\beta}\st(z;\alpha,\beta)}
  &\;=\;\Gamma(\beta)^{-1}\frac{\partial^n}{\partial z^n}\paren{e^{-z}\sigma(z;\alpha,\beta)}\\
  &\overset{(\ref{eq:n_derivada_st})}{=}\Gamma(\beta)^{-1}(-1)^ne^{-z}\sigma(z;\alpha+n,\beta)\\
  &\;=\;(-1)^ne^{-z}z^{-\beta}\,\st(z;\alpha+n,\beta),
\end{align*}
o equivalentemente
\begin{equation}\label{eq:derivada_st}
  \st(z;\alpha,\beta)=
  (-1)^ne^zz^{\beta}\frac{\partial^n}{\partial z^n}\paren{e^{-z}z^{-\beta}\st(z;\alpha-n,\beta)}.
\end{equation}

Ahora podemos volver a extender $\st$, pero esta vez a todo $\HH'\times\CC\times\CC$. Tomamos la
extensi\'on (\ref{eq:semi_ext_st}) de $\st$ y usamos (\ref{eq:derivada_st}) para obtener:
\[
   \st(z;\alpha,\beta)=
  \begin{cases}
    \st(z;1-\beta,1-\alpha) & \Re(\alpha)<1,\beta\in\CC\\
    (-1)^ne^zz^{\beta}\frac{\partial^n}{\partial z^n}\paren{e^{-z}z^{-\beta}\st(z;\alpha-n,\beta)}
    & \Re(\alpha)\geq 1,\beta\in\CC\quad\big(n=\lfloor\Re(\alpha)\rfloor\big)
  \end{cases}
\]
donde $\lfloor\Re(\alpha)\rfloor$ se define como el menor natural $n$ tal que
$n\leq \Re(\alpha)<n+1$. Con esta nueva definici\'on, $\st$ es una funci\'on bien definida
y holomorfa sobre $\HH'\times\CC\times\CC$. Con esto podemos conlcuir que
$\sigma(z;\alpha,\beta)=\Gamma(\beta)z^{-\beta}\st(z;\alpha,\beta)$ se extiende a una funci\'on
meromorfa sobre $\HH'\times\CC\times\CC$. 

Para probar la tercera afirmaci\'on del lema, dividimos en dos casos: primero asumimos que
$Q\subset\CC\times\HH'$ y despu\'es usamos esto para probar el caso cuando
$Q$ es un subconjunto compacto arbitrario de $\CC\times\CC$.
\begin{itemize}
\item[$(Q\subset\CC\times\HH')$] Sea $(\alpha,\beta)\in Q$, entonces $\Re(\alpha)$ est\'a acotado;
  sea $n\in\NN$ tal que $\Re(\alpha)\leq n+1$. Como $\Re(\beta)>0$
\item[$(Q\subset\CC\times\CC)$]
\end{itemize}
\end{proof}














\end{proof}

Sustituimos el resultado del teorema \ref{thm:S_holomorfo} en (\ref{eq:E_en_terminos_de_S}) y
  escribimos $z=x+iy$:
\begin{align*}
  E_{1,\chi}(z,s)&=2\sum_{m=1}^{\infty}\chi(m)m^{-1-2s}\sum_{r=0}^{m-1}
                   S\paren{z+\tfrac{r}{m};1+s,s}!!??intercambiar!!??\\
                 &=2\sum_{m=1}^{\infty}\chi(m)m^{-1-2s}\sum_{r=0}^{m-1}
                   \sum_{n=-\infty}^{\infty}\hat{\phi}(n,y;1+s,s)e^{2\pi in(x+\tfrac{r}{m})}\\
                 &=2\sum_{n=-\infty}^{\infty}\hat{\phi}(n,y;1+s,s)e^{2\pi in x}\sum_{m=1}^{\infty}
                   \chi(m)m^{-1-2s}\underset{*}{\underbrace{\sum_{r=0}^{m-1}e^{2\pi inr/m}}}
\end{align*}
Observa que que la suma $(*)$ es simplemente una suma geom\'etrica:
\[
  \sum_{r=0}^{m-1}e^{2\pi inr/m}=
  \begin{cases}
    0 &\text{si}\;\; m\not\,\mid n\\
    m &\text{si}\;\; m\mid n
  \end{cases}
\]
Por lo tanto:
\begin{align*}
  E_{1,\chi}(z,s)&=2\sum_{n=-\infty}^{\infty}\hat{\phi}(n,y;1+s,s)e^{2\pi in x}\sum_{m\mid n}\chi(m)m^{-2s}
\end{align*}









\section{Curvas Elipticas}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% CURVAS ELIPTICAS

\section{Representaciones de Galois}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% REPRESENTACIONES DE GALOIS

Sea $E$ una curva el\'iptica sobre $\QQ$ y $E[p]$ sus puntos de orden $p$, un primo. Observa que
el grupo de Galois absoluto $G_{\QQ}:=\text{Gal}(\bar{\QQ}|\QQ)$ act\'ua sobre $E$ y en particular
act\'ua sobre $E[p]$ de la siguiente manera:
\[
  G_{\QQ}\curvearrowright E[p] \qquad\text{con}\qquad \sigma P \mapsto P^{\sigma}.
\]
Esta acci\'on est\'a bien definida porque la acci\'on de $G_{\QQ}$ conmuta con la suma de $E$. En
efecto, si $P$ y $Q$ son dos puntos de $E$, entonces las coordenadas de
$P+Q$ son funciones racionales en las coordenadas de $P$ y $Q$
\cite[\S III.2, Group Law Algorithm]{SilvermanTAOEC}. Por lo tanto
\[
  O=O^{\sigma}=([p]P)^{\sigma}=(P+\cdots+P)^{\sigma}=P^{\sigma}+\cdots+P^{\sigma}=[p]P^{\sigma}
\]
y as\'i $P^{\sigma}\in E[p]$ siempre que $P\in E[p]$. De esta manera cada $\sigma$ induce
un automorfismo de $E[p]$, es decir, tenemos una representaci\'on $G_{\QQ}\ra\text{Aut}(E[p])$.
Por otro lado, sabemos que $E[p]\cong\FF_p\times\FF_p$ como $\FF_p$-espacios vectoriales.
Por lo tanto Aut$(E[p])$ es simplemente $\GL_2(\FF_p)$. As\'i definimos:

\begin{defin}
  La representaci\'on de Galois de los puntos de $p$-torsi\'on de una curva el\'iptica $E$
  se denota por
  \[
    \bar{\rho}_{E,p}:G_{\QQ}\lra \GL_2(\FF_p)
  \]
\end{defin}

\begin{nota}
  El isomorfismo Aut$(E[p])\cong\FF_p\times\FF_p$ no es natural, depende de la $\FF_p$-base
  que elijas para $E[p]$.
\end{nota}

Esta representaci\'on cumple dos propiedades muy importantes que escribimos como teoremas:

\begin{thm}\label{thm:det_de_rhop}
  Si escribimos $\chi_p:G_{\QQ}\ra$Aut$(\mu_p)\cong\FF_p^\times$ como el caracter
  ciclot\'omico, donde $\mu_p\subseteq\CC^{\times}$ denota a las raices $p$-\'esimas de la unidad.
  Entonces
  \[
    \det\rhop =\chi_p.
  \]
\end{thm}
\begin{proof}
  Para ver esto, recuerda que el \emph{Weil pairing} $e_p:E[p]\times E[p]\ra\mu_p$ es una
  forma bilineal, alternante, no-degenerada e invariante bajo la acci\'on de $G_{\QQ}$
  \cite[\S III.8, proposici\'on 8.1]{SilvermanTAOEC}.
\end{proof}

\begin{thm}\label{thm:traza_rhop}
  $$\tr\bar{\rho}_{E,p}(\Fr_q)$$
\end{thm}
    




\end{document}
